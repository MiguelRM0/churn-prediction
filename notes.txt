Helpful PostgreSQL commands: pgdata is the database cluster
pg_ctl -D ~/pgdata status
pg_ctl -D ~/pgdata start
pg_ctl -D ~/pgdata stop


How to run a python file
Ex.
    python -m src.data.load_db

-- 
in Backend to run FastAPI
uvicorn main:app --reload
-- 

---
FASTAPI is my server 
"my laptop is now a web server"
---

----
Mental Model to Keep Forever

Routes handle logic.
Mounted apps handle responsibilities.

If something:

Has its own job

Handles many requests

Shouldn’t mix with business logic

➡️ It probably gets mounted.
----

-----
Running It:
Start PostgreSQL: pg_ctl -D ~/pgdata start
Run backend: cd backend && uvicorn main:app --reload
Frontend accessible at http://localhost:8000/
-----


